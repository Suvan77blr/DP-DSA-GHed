{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile and ru${lineNumber}",
            "type": "shell",
            "command": "g++ -o ${fileBasenameNoExtension}.exe ${file} 2> output.exe && ./${fileBasenameNoExtension}.exe < input.txt > output.txt 2> output.txt",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}